{
  "name": "personal-finance-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace @personal-finance/shared",
    "dev:web": "npm run build:shared && npm run dev --workspace web",
    "dev:api": "npm run build:shared && npm run dev --workspace api",
    "build": "npm run build:shared && npm run build --workspace api && npm run build --workspace web",
    "lint": "npm run lint --workspace web",
    "check": "npm run build:shared && npm run check --workspace api && npm run check --workspace web"
  },
  "engines": {
    "node": ">=20.17.0"
  }
}
